# Jun's Blog

개인 기술 블로그 웹사이트입니다. Gatsby.js와 TypeScript를 사용하여 구축되었으며, GitHub Pages를 통해 배포됩니다.

## 🚀 주요 기능

- **블로그 포스트**: Markdown 기반의 기술 블로그 포스트 작성 및 관리
- **수학 공식 지원**: LaTeX/Katex를 통한 수학 공식 렌더링
- **다크/라이트 테마**: 사용자 선호에 따른 테마 전환
- **뉴스 테이블**: 최신 뉴스 정보 표시
- **논문 테이블**: 연구 논문 정보 관리
- **루렛 기능**: 인터랙티브 룰렛 컴포넌트
- **반응형 디자인**: 다양한 디바이스에서 최적화된 UI/UX

## 🛠️ 기술 스택

- **프레임워크**: Gatsby.js 5.2.0
- **언어**: TypeScript
- **UI 라이브러리**: Ant Design 5.0.5
- **스타일링**: CSS, Emotion
- **수학 공식**: KaTeX
- **배포**: GitHub Pages
- **CI/CD**: GitHub Actions

## 📦 설치 및 실행

### 사전 요구사항

- Node.js 18 이상
- npm 또는 yarn

### 설치

```bash
# 의존성 설치
npm install
```

### 개발 서버 실행

```bash
# 개발 모드로 실행 (http://localhost:8000)
npm run develop
# 또는
npm start
```

### 빌드

```bash
# 프로덕션 빌드
npm run build

# 빌드된 사이트 미리보기
npm run serve
```

### 환경 변수 설정

`.env.production` 파일을 생성하고 다음 변수를 설정하세요:

```env
GATSBY_NEWS_API_KEY=your_news_api_key
```

## 📁 프로젝트 구조

```
.
├── src/
│   ├── components/      # 재사용 가능한 컴포넌트
│   │   ├── header.tsx
│   │   ├── layout.tsx
│   │   ├── news-table.tsx
│   │   ├── paper-table.tsx
│   │   ├── roulette.tsx
│   │   └── seo.tsx
│   ├── context/         # React Context (테마 등)
│   ├── data/            # 정적 데이터 파일
│   ├── images/          # 이미지 리소스
│   ├── pages/           # 페이지 컴포넌트
│   ├── posts/           # Markdown 블로그 포스트
│   ├── styles/          # CSS 스타일 파일
│   └── templates/       # 페이지 템플릿
├── gatsby-config.js     # Gatsby 설정
├── gatsby-node.js       # Gatsby Node API
├── package.json
└── tsconfig.json        # TypeScript 설정
```

## 📝 블로그 포스트 작성

`src/posts/` 디렉토리에 Markdown 파일을 추가하면 자동으로 블로그 포스트로 생성됩니다.

### 포스트 작성 예시

```markdown
---
title: "포스트 제목"
date: "2026-01-23"
---

포스트 내용...

수학 공식은 다음과 같이 작성할 수 있습니다:
$E = mc^2$
```

## 🚢 배포

이 프로젝트는 GitHub Actions를 통해 자동으로 배포됩니다.

1. `master` 브랜치에 푸시하면 자동으로 빌드 및 배포가 시작됩니다
2. 빌드된 파일은 `gh-pages` 브랜치에 배포됩니다
3. 사이트는 `https://kj2063.github.io/`에서 확인할 수 있습니다

### 수동 배포

```bash
# 배포 (자동으로 predeploy가 실행되어 빌드 후 배포)
npm run deploy
```

`deploy` 스크립트는 `predeploy` 훅을 자동으로 실행하여 빌드를 수행한 후 `gh-pages`에 배포합니다.

## 🧪 개발 스크립트

프로젝트에서 사용 가능한 npm 스크립트입니다:

```bash
# 프로덕션 빌드
npm run build
# → gatsby build 실행

# 개발 서버 실행 (http://localhost:8000)
npm run develop
# → gatsby develop 실행

# 개발 서버 실행 (develop의 별칭)
npm start
# → gatsby develop 실행

# 코드 포맷팅 (Prettier)
npm run format
# → prettier --write "**/*.{js,jsx,ts,tsx,json,md,css}"

# 빌드된 사이트 미리보기
npm run serve
# → gatsby serve 실행

# Gatsby 캐시 정리
npm run clean
# → gatsby clean 실행

# 테스트 실행
npm test
# → 현재는 플레이스홀더 (테스트 구현 필요)
# → https://gatsby.dev/unit-testing 참고

# 배포 전 빌드 (predeploy 훅)
npm run predeploy
# → npm run build 실행 (deploy 전 자동 실행)

# GitHub Pages에 배포
npm run deploy
# → gh-pages -d public 실행
# → predeploy 훅이 자동으로 실행되어 빌드 후 배포
```

## 📄 라이선스

이 프로젝트는 0BSD 라이선스를 따릅니다.

## 👤 작성자

**권준 (Jun Kwon)**

- GitHub: [@kj2063](https://github.com/kj2063)
- LinkedIn: [Jun Kwon](https://www.linkedin.com/in/jun-kwon-556246302/)
- Email: kwonjun8890@gmail.com

## 🙏 감사의 말

이 프로젝트는 [Gatsby](https://www.gatsbyjs.com/)를 기반으로 구축되었습니다.
